function pares(x, y) {  
    //verifica se x e par (se nao , soma 1)
    if(x % 2 != 0 ){
        x++; // passa a ser par
    }
    //garante que x é par
    while(x <= y){
        console.log(x);
        x += 2; //soma 2 para o proximo continuar a ser par
    }
}

pares(32,321);
